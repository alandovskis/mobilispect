viewModelScope = main, but can run suspend functions
When creating a coroutine from a non-coroutine, start with launch.
Coroutines Codelab Retrofit = suspend support
withContext to choose which thread pool
switchMap to change between LiveData - Flow equivalent to flatmapLatest
Flow::launchIn: collect all values
Flow::onEach: every time Flow returns a value

